<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff"
    tools:context="com.jsxl.selfexam.fragment.RecordDetailFragment">
    <com.jsxl.selfexam.widget.HorizontalListView
        android:id="@+id/record_hor_listView"
        android:layout_width="match_parent"
        android:scrollbars="none"
        android:layout_height="50dp">

    </com.jsxl.selfexam.widget.HorizontalListView>
    <ListView
        android:layout_below="@id/record_hor_listView"
        android:id="@+id/record_listView"
        android:scrollbars="none"
        android:dividerHeight="0.5dp"
        android:divider="#f1f1f1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
    </ListView>
    <RelativeLayout
        android:visibility="gone"
        android:id="@+id/rl_empty"
        android:layout_below="@id/record_hor_listView"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <ImageView
            android:id="@+id/iv_empty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:src="@drawable/icon_record_empty" />
        <TextView
            android:id="@+id/tv_empty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="#333333"
            android:layout_marginTop="8dp"
            android:layout_centerHorizontal="true"
            android:layout_below="@id/iv_empty"
            android:text="暂无记录"/>
    </RelativeLayout>
</RelativeLayout>
